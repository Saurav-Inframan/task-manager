# Managed by Inframan — generated from console config. Edits here are the source of truth.
version: v1
# immutable — managed by Inframan, do not edit
application: task-manager-saurav
services:
    task-manager:
        type: web
        port: 8000
        domain: task-manager.inframan.cloud
        path_prefix: /saurav
        path_rewrite: /saurav/
        node_pool: application-pool
        scaling:
            type: concurrency
            min_replicas: 0
            max_replicas: 1
            target: 100
        resources:
            cpu_request: 100m
            memory_request: 128Mi
        build:
            method: docker
            dockerfile: ./Dockerfile
        path: ""
